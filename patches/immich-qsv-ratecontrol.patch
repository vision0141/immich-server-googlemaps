diff --git a/i18n/en.json b/i18n/en.json
index fdf3a21..c3d8f12 100644
--- a/i18n/en.json
+++ b/i18n/en.json
@@ -404,8 +404,8 @@
     "transcoding_settings_description": "Manage which videos to transcode and how to process them",
     "transcoding_target_resolution": "Target resolution",
     "transcoding_target_resolution_description": "Higher resolutions can preserve more detail but take longer to encode, have larger file sizes, and can reduce app responsiveness.",
-    "transcoding_temporal_aq": "Temporal AQ",
-    "transcoding_temporal_aq_description": "Applies only to NVENC. Temporal Adaptive Quantization increases quality of high-detail, low-motion scenes. May not be compatible with older devices.",
+    "transcoding_temporal_aq": "Temporal AQ / QSV Look Ahead",
+    "transcoding_temporal_aq_description": "Applies only to NVENC. Temporal Adaptive Quantization increases quality of high-detail, low-motion scenes. May not be compatible with older devices. For QSV AV1, this toggle is also used to enable extbrc/look-ahead.",
     "transcoding_threads": "Threads",
     "transcoding_threads_description": "Higher values lead to faster encoding, but leave less room for the server to process other tasks while active. This value should not be more than the number of CPU cores. Maximizes utilization if set to 0.",
     "transcoding_tone_mapping": "Tone-mapping",
diff --git a/i18n/ja.json b/i18n/ja.json
index cd98b39..07e3dbc 100644
--- a/i18n/ja.json
+++ b/i18n/ja.json
@@ -404,8 +404,8 @@
     "transcoding_settings_description": "トランスコードする動画とその処理方法を管理します",
     "transcoding_target_resolution": "解像度",
     "transcoding_target_resolution_description": "解像度を高くすると細かなディテールを保持できますが、エンコードに時間がかかり、ファイルサイズが大きくなり、アプリの応答性が低下する可能性があります。",
-    "transcoding_temporal_aq": "適応的量子化(Temporal AQ)",
-    "transcoding_temporal_aq_description": "NVEncにのみ適用されます。Temporal AQは高精細で動きの少ないシーンの画質を向上させます。古いデバイスとの互換性はありません。",
+    "transcoding_temporal_aq": "適応的量子化(Temporal AQ) / QSV Look Ahead",
+    "transcoding_temporal_aq_description": "NVEncにのみ適用されます。Temporal AQは高精細で動きの少ないシーンの画質を向上させます。古いデバイスとの互換性はありません。QSV AV1 では extbrc/look-ahead の切り替えにも利用されます。",
     "transcoding_threads": "スレッド数",
     "transcoding_threads_description": "値を高くするとエンコード速度が速くなりますが、アクティブな間はサーバーが他のタスクを処理する余裕が少なくなります。この値はCPUのコア数を超えないようにする必要があります。\"0\" に設定すると、最大限利用されます。",
     "transcoding_tone_mapping": "トーンマッピング",
diff --git a/server/src/services/media.service.spec.ts b/server/src/services/media.service.spec.ts
index 51a10a3..558eeba 100644
--- a/server/src/services/media.service.spec.ts
+++ b/server/src/services/media.service.spec.ts
@@ -2842,7 +2842,7 @@ describe(MediaService.name, () => {
             '-v verbose',
             '-vf hwupload=extra_hw_frames=64,scale_qsv=-1:720:mode=hq:format=nv12',
             '-preset 7',
-            '-global_quality:v 23',
+            '-b:v 6897k',
             '-maxrate 10000k',
             '-bufsize 20000k',
           ]),
@@ -2851,6 +2851,99 @@ describe(MediaService.name, () => {
       );
     });
 
+    it('should keep icq quality mode for qsv when max bitrate is enabled', async () => {
+      mocks.media.probe.mockResolvedValue(probeStub.matroskaContainer);
+      mocks.systemMetadata.get.mockResolvedValue({
+        ffmpeg: { accel: TranscodeHardwareAcceleration.Qsv, cqMode: CQMode.Icq, maxBitrate: '10000k' },
+      });
+
+      await sut.handleVideoConversion({ id: 'video-id' });
+
+      expect(mocks.media.transcode).toHaveBeenCalledWith(
+        '/original/path.ext',
+        expect.any(String),
+        expect.objectContaining({
+          outputOptions: expect.arrayContaining(['-global_quality:v 23']),
+        }),
+      );
+      expect(mocks.media.transcode).toHaveBeenCalledWith(
+        '/original/path.ext',
+        expect.any(String),
+        expect.objectContaining({
+          outputOptions: expect.not.arrayContaining(['-b:v 6897k', '-maxrate 10000k', '-bufsize 20000k']),
+        }),
+      );
+    });
+
+    it('should keep cqp mode for qsv when max bitrate is enabled', async () => {
+      mocks.media.probe.mockResolvedValue(probeStub.matroskaContainer);
+      mocks.systemMetadata.get.mockResolvedValue({
+        ffmpeg: { accel: TranscodeHardwareAcceleration.Qsv, cqMode: CQMode.Cqp, maxBitrate: '10000k' },
+      });
+
+      await sut.handleVideoConversion({ id: 'video-id' });
+
+      expect(mocks.media.transcode).toHaveBeenCalledWith(
+        '/original/path.ext',
+        expect.any(String),
+        expect.objectContaining({
+          outputOptions: expect.arrayContaining(['-q:v 23']),
+        }),
+      );
+      expect(mocks.media.transcode).toHaveBeenCalledWith(
+        '/original/path.ext',
+        expect.any(String),
+        expect.objectContaining({
+          outputOptions: expect.not.arrayContaining(['-b:v 6897k', '-maxrate 10000k', '-bufsize 20000k']),
+        }),
+      );
+    });
+
+    it('should enable qsv look-ahead for av1 when temporal AQ is enabled', async () => {
+      mocks.media.probe.mockResolvedValue(probeStub.matroskaContainer);
+      mocks.systemMetadata.get.mockResolvedValue({
+        ffmpeg: {
+          accel: TranscodeHardwareAcceleration.Qsv,
+          targetVideoCodec: VideoCodec.Av1,
+          maxBitrate: '10000k',
+          temporalAQ: true,
+        },
+      });
+
+      await sut.handleVideoConversion({ id: 'video-id' });
+
+      expect(mocks.media.transcode).toHaveBeenCalledWith(
+        '/original/path.ext',
+        expect.any(String),
+        expect.objectContaining({
+          outputOptions: expect.arrayContaining(['-extbrc 1', '-look_ahead_depth 30']),
+        }),
+      );
+    });
+
+    it('should not enable qsv look-ahead when qsv quality mode is explicit', async () => {
+      mocks.media.probe.mockResolvedValue(probeStub.matroskaContainer);
+      mocks.systemMetadata.get.mockResolvedValue({
+        ffmpeg: {
+          accel: TranscodeHardwareAcceleration.Qsv,
+          targetVideoCodec: VideoCodec.Av1,
+          maxBitrate: '10000k',
+          temporalAQ: true,
+          cqMode: CQMode.Icq,
+        },
+      });
+
+      await sut.handleVideoConversion({ id: 'video-id' });
+
+      expect(mocks.media.transcode).toHaveBeenCalledWith(
+        '/original/path.ext',
+        expect.any(String),
+        expect.objectContaining({
+          outputOptions: expect.not.arrayContaining(['-extbrc 1', '-look_ahead_depth 30']),
+        }),
+      );
+    });
+
     it('should set options for qsv with custom dri node', async () => {
       mocks.media.probe.mockResolvedValue(probeStub.matroskaContainer);
       mocks.systemMetadata.get.mockResolvedValue({
diff --git a/server/src/utils/media.ts b/server/src/utils/media.ts
index ce18530..caa3f42 100644
--- a/server/src/utils/media.ts
+++ b/server/src/utils/media.ts
@@ -683,6 +683,9 @@ export class QsvSwDecodeConfig extends BaseHWConfig {
     if (this.config.targetVideoCodec === VideoCodec.Vp9) {
       options.push('-low_power 1');
     }
+    if (this.shouldUseLookAhead()) {
+      options.push('-extbrc 1', '-look_ahead_depth 30');
+    }
     return options;
   }
 
@@ -705,12 +708,20 @@ export class QsvSwDecodeConfig extends BaseHWConfig {
   }
 
   getBitrateOptions() {
-    const options = [`-${this.useCQP() ? 'q:v' : 'global_quality:v'} ${this.config.crf}`];
     const bitrates = this.getBitrateDistribution();
-    if (bitrates.max > 0) {
-      options.push(`-maxrate ${bitrates.max}${bitrates.unit}`, `-bufsize ${bitrates.max * 2}${bitrates.unit}`);
+
+    // QSV quality-based modes switch away from ICQ/CQP once maxrate is supplied
+    // without also selecting a bitrate mode. Keep explicit ICQ/CQP settings intact,
+    // and only use bitrate mode when CQ mode is left on auto.
+    if (bitrates.max > 0 && this.config.cqMode === CQMode.Auto) {
+      return [
+        `-b:v ${bitrates.target}${bitrates.unit}`,
+        `-maxrate ${bitrates.max}${bitrates.unit}`,
+        `-bufsize ${bitrates.max * 2}${bitrates.unit}`,
+      ];
     }
-    return options;
+
+    return [`-${this.useCQP() ? 'q:v' : 'global_quality:v'} ${this.config.crf}`];
   }
 
   getSupportedCodecs() {
@@ -736,6 +747,15 @@ export class QsvSwDecodeConfig extends BaseHWConfig {
     return this.config.cqMode === CQMode.Cqp || this.config.targetVideoCodec === VideoCodec.Vp9;
   }
 
+  shouldUseLookAhead() {
+    return (
+      this.config.temporalAQ &&
+      this.config.targetVideoCodec === VideoCodec.Av1 &&
+      this.config.cqMode === CQMode.Auto &&
+      this.getMaxBitrateValue() > 0
+    );
+  }
+
   getScaling(videoStream: VideoStreamInfo): string {
     return super.getScaling(videoStream, 1);
   }
